// strchr(string, 'char');

// strrchr(string, 'char');

// strstr(string, "str");

// strcmp(string1, string2);

// strcpy(string1, string2);

// strncpy(string1, string2, n);

// strcat(string1, string2);

// strncat(string1, string2, n);

// swap(a, b)     a and b must be the same type!

// string.size()     return lenght of string

// sizeof(string)     return size of string in Byte

// string.pop_back()     remove the last character of the string

// min(a, b)
// max(a, b)
// min(a, b, cmp)     we can create a bool compare function (it returns true if a be definitely greater than b
//                                                      and retuns false if a be equal or less than b)

// ans(int)

// sqrt(int)

// cout << fixed << setprecision(10);     need <iomapip> library

// #include <bits/stdc++.h>

// setfill('0') << setw(2);     for use u need <iomanip> library

// sort(coins, coins + n, greater<int>());     need <algorithm> library

// s.insert(index, "string")

// s.erase(index, 2)     delet index and two next charracters

// s.replace(12, 5, "string")    replase string in s on index 12 to 12+5

//  s.resize(4, '?')

// template <typename N>
// N max(N x, N y, N z){ ...}

// cout << numeric_limits<int>::max();     need <limits> library

// time_t now = time(0);
// char * ch = ctime(&now);
// cout << ch;               need <ctime> library

// sleep(500)        500 is in miliseconds
// system("cls")
// system("color ")       need <windows.h>

// int x;
// cin >> x;
// int * myArray;
// myArray = new int[x];
// ...
// delete[] myArray;

// my_str.erase(remove(my_str.begin(), my_str.end(), 'A'), my_str.end()); //remove A from string

// template<class type>ret-type func-name(parameter list){
// body of the function}

// can replace 'class' keyword by 'typename'

// typename(an-value).name();